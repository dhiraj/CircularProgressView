<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.traversient.circularprogressview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CircularProgressView">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AnimationActivity"
            android:theme="@style/AppTheme.AnimatedCircular" />
        <activity
            android:name=".DashEffectActivity"
            android:theme="@style/AppTheme.DashCircular" />
        <activity
            android:name=".BasicCircularBarActivity"
            android:theme="@style/AppTheme.BasicCircular" />

    </application>

</manifest>